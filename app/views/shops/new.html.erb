<p>新規登録</p>
<%= form_with model: @user, local: true do |f| %>
  <p>名前：<%= f.text_field :name %></p>
  <p>日付：<%= f.date_select :date %></p>
  <p>内容：<%= f.text_field :content %></p>
  <%= f.submit "送信" %>
<% end %>
